const request = require('supertest');
const should = require('should');
const app = require('../app');
// const models = require('../models/mongo');

describe.skip('GET 식당 정보 조회', () => {
    describe('성공시', () => {
        // let response;
        // before((done) => {
        //     request(app)
        //         .get('/api/v1/exchanges')
        //         .expect(200)
        //         .end((err, res) => {
        //             if (err) throw err;
        //             response = res;
        //             done();
        //         });
        // });
        // it('식당 객체를 담은 배열을 응답한다', () => {
        //     response.body.should.be.instanceof(Array);
        //     response.body.forEach(exchange => assertExchange(exchange));
        // });
    });
});
